{
  "mcpServers": {
    "x-twitter": {
      "command": "sh",
      "args": [
        "-c",
        "X_API_KEY=$(security find-generic-password -a formrecap -s x-api-consumer-key -w) X_API_SECRET=$(security find-generic-password -a formrecap -s x-api-consumer-secret -w) X_BEARER_TOKEN=$(security find-generic-password -a formrecap -s x-api-bearer-token -w) X_ACCESS_TOKEN=$(security find-generic-password -a formrecap -s x-api-access-token -w) X_ACCESS_TOKEN_SECRET=$(security find-generic-password -a formrecap -s x-api-access-token-secret -w) node /Users/jasonmatthew/.claude/mcp-servers/x-mcp/dist/index.js"
      ]
    },
    "reddit": {
      "command": "npx",
      "args": ["reddit-mcp-server"],
      "env": {
        "REDDIT_AUTH_MODE": "anonymous",
        "REDDIT_SAFE_MODE": "standard"
      }
    }
  }
}
