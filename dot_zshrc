# Oh My Zsh
export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME=""
plugins=(git npm node zsh-autosuggestions zsh-syntax-highlighting docker docker-compose gcloud)
source $ZSH/oh-my-zsh.sh

# mise — version manager (replaces Volta + pyenv)
eval "$(mise activate zsh)"

# PATH — Google Cloud SDK
export PATH="$HOME/google-cloud-sdk/bin:$PATH"
source "$HOME/google-cloud-sdk/completion.zsh.inc" 2>/dev/null

# PATH — LM Studio
export PATH="$PATH:$HOME/.cache/lm-studio/bin"

# Secrets from macOS Keychain
_ks() { security find-generic-password -s "zsh-secrets" -a "$1" -w 2>/dev/null; }
export TURBO_TOKEN=$(_ks TURBO_TOKEN)
export TURBO_TEAM=$(_ks TURBO_TEAM)
export TURBO_REMOTE_CACHE_SIGNATURE_KEY=$(_ks TURBO_REMOTE_CACHE_SIGNATURE_KEY)

# Aliases
alias zshconfig="code ~/.zshrc"
alias ohmyzsh="code ~/.oh-my-zsh"
alias code="/opt/homebrew/bin/code-insiders"
alias c="code ."
alias vim="nvim"
alias vi="nvim"
alias g="git"

# bun completions
[ -s "/Users/jasonmatthew/.bun/_bun" ] && source "/Users/jasonmatthew/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

alias claude-mem='bun "/Users/jasonmatthew/.claude/plugins/marketplaces/thedotmack/plugin/scripts/worker-service.cjs"'
export PATH="$HOME/.local/bin:$PATH"

# Refresh the cached Moonshot key from 1Password (run when key is rotated; one Touch ID prompt)
alias refresh-moonshot='op read "op://Private/Moonshot AI/credential" --no-newline > ~/.cache/moonshot-key && chmod 600 ~/.cache/moonshot-key && echo "moonshot key refreshed ($(wc -c < ~/.cache/moonshot-key) bytes)"'

# Starship prompt
eval "$(starship init zsh)"
