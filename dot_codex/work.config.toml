# Explicit native baseline alias; base permissions and hooks remain active.
model = "gpt-6-astra"
model_reasoning_effort = "medium"
developer_instructions = """
Inside Herdr (HERDR_ENV=1), name your own root agent for the first substantive task and update it when the goal changes. Before `herdr agent rename`, verify `herdr pane current --current` reports agent `codex`, plus nonempty CODEX_THREAD_ID and agent_session.value that match; use that pane_id, skip missing values or any mismatch, and never name a parent or another agent. Keep native Herdr lifecycle detection intact.
"""
